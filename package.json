{
  "name": "mesh-alert-system",
  "version": "1.0.0",
  "description": "Mesh-based Alerting System with Localization Support (Team: The Inevitables)",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix client install",
    "build:client": "npm --prefix client run build",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server start",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "test:tcp": "npm --prefix server run test-tcp"
  },
  "keywords": [
    "ble-mesh",
    "emergency-alert",
    "tcp-socket",
    "socket.io",
    "react"
  ],
  "author": "The Inevitables"
}
